{
  "name": "webhtv-playback-sync",
  "version": "1.0.0",
  "private": true,
  "description": "WebHTV 观影记录同步服务端 (Cloudflare Workers + KV)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
